Health Radar v1.6.4 — Report Dedup Fix

Виправлено:
- у PDF/друкованому звіті “Тригери/події” більше не дублюються однакові записи;
- дублі визначаються за: хвилина + тип події + значення + примітка + extra;
- дані у Firebase не видаляються, виправлення стосується тільки формування звіту;
- збережені синхронізація та попередні hotfix.

Заміна:
Завантажити всі файли в GitHub. Обовʼязково app.js, index.html, sw.js.
Після оновлення Ctrl+F5 / очистити кеш на телефоні.
